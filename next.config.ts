// created and pre-configured when you start a new project using create-next-app
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
